"""The catalog-derived regulator capability available to public consumers."""
enum RegulatorCapability {
  UNKNOWN
  NONE
  PRESENT
}

"""Additive excerpt from the gateway's existing Query type."""
type Query {
  regulator_capability: RegulatorCapability!
}
